"""
Sketchatone native (Kivy) UI package.

Importing this package does not import Kivy; submodules (notably
``sketchatone.ui.app``) import Kivy lazily so the CLI surface stays
usable without the optional ``[ui]`` extra installed.
"""
