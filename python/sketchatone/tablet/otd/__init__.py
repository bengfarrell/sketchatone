"""Python port of the OpenTabletDriver report-parsing layer.

The vendored JSON configurations under ``configs/`` and the parser classes
under ``parsers/`` are ports / copies of the corresponding C# code in
OpenTabletDriver.Configurations. See ``configs/VENDORED_FROM.txt``.
"""
